## Dev environment tips
- You are using OpenShift cluster!

## Testing instructions
- Use `oc` instead of `kubectl` to interact with OpenShift cluster!


## PR instructions
- Title format: [<project_name>] <Title>
